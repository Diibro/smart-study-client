import React from 'react'
import IButton from '../../interfaces/client/IButton'

const AddButton:React.FC<IButton> = () => {
     return (
     <button className='dash-add-button'  />
     )
}

export default AddButton